# V1 contract fixtures

This directory is reserved for stable, non-sensitive fixtures shared by V1
contract tests. Foundation tests currently construct minimal test doubles in
Go so no product file-format implementation is implied.

Future fixtures must be deterministic, small, and derived. Source datasets
outside this repository remain immutable.
