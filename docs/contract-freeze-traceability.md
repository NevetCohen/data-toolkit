# V1 Contract Freeze Traceability Base

Source change: `implement-revised-data-toolkit-v1`

This is an extraction-only inventory. It maps neither rows to tasks nor implementation artifacts. `normative_criterion` is a compact rendering of the normative prose or the exact scenario outcome at the cited heading.

| spec | requirements | scenarios | total |
|---|---:|---:|---:|
| application-api | 10 | 11 | 21 |
| cli-json-surface | 6 | 6 | 12 |
| codex-adapter | 14 | 16 | 30 |
| infrastructure-contracts | 13 | 17 | 30 |
| logical-operations | 11 | 14 | 25 |
| reader-engine | 8 | 9 | 17 |
| transform-workflow | 10 | 11 | 21 |
| v1-acceptance | 12 | 13 | 25 |
| writer-engine | 6 | 6 | 12 |
| **total** | **90** | **103** | **193** |

| spec | row_kind | name | source_anchor | normative_criterion |
|---|---|---|---|---|
+
## Anchor integrity

- Extracted heading anchors: 193
- Duplicate anchors: none
- Missing anchors: none

## OS-readiness-v1.1 change-scoped precedence

For this change, the later approved OS-readiness-v1.1 decision supersedes the
revised source brief line 32 full-cleanup-replacement wording. Exactly four
mapped behaviors are V1 coverage through the fixed ten-operation catalog and
fourteen behaviors are `outside_v1`; no replacement or equivalence claim is
made. The source brief remains unchanged because it is outside this run's
authorized write scope.

+Exit code: 0
Wall time: 1.4 seconds
Output:
| writer-engine | requirement | Explicit Writer Engine registry | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:3 | The Writer Engine SHALL register versioned writer-format descriptors and concrete implementations for rendering, staging, optional styling, reopened validation, publication, and terminal finalization. It SHALL publish only implemented writer capabilities. |
| writer-engine | scenario | Descriptor and implementation mismatch | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:9 | - **WHEN** a writer descriptor declares a capability without its required implementation - **THEN** registration fails before the capability becomes discoverable |
| writer-engine | requirement | Staged, reopened, validated output | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:13 | `writer.write` SHALL render the final canonical stream to a staging path outside the final target. `writer.style` is optional and Excel-only. `writer.validate` SHALL close the staged artifact, reopen it, validate its format and declared schema, and then evaluate requested assertions in request order. No artifact that fails any validation may be published. |
| writer-engine | scenario | Output validation fails | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:20 | - **WHEN** format, schema, or a user assertion fails after staging - **THEN** no final output is published and finalization follows the retention policy |
| writer-engine | requirement | Publication and terminal finalization | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:24 | `writer.publish` SHALL apply local `block`, `overwrite`, or `alternate_name` collision policy only after validation. `writer.finalize` SHALL run exactly once for every attempted run after its workspace exists, including reader failure, logical failure, writer failure, and cancellation. It SHALL close the registered resources in reverse acquisition order, ... |
| writer-engine | scenario | Cancellation during streaming | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:34 | - **WHEN** cancellation occurs after a run workspace exists - **THEN** no output is published, `writer.finalize` closes resources and records one cancelled receipt |
| writer-engine | requirement | Exact writer request and descriptor structures | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:38 | The Writer Engine SHALL use the following exact strict request and descriptor structures. Output paths SHALL be normalized to absolute paths before request hashing and comparison with the source path. `OutputRequest` SHALL reject every format-specific field that does not apply to its selected format. Runtime discovery SHALL publish only a descriptor whose... |
| writer-engine | scenario | Output equals source | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:67 | - **WHEN** an output request normalizes to the same local path as the source - **THEN** strict request validation rejects it before staging is created |
| writer-engine | requirement | Exact staged artifact and validation structures | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:71 | The Writer lifecycle SHALL use the following exact internal/result structures. |
| writer-engine | scenario | Publication input | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:84 | - **WHEN** publication starts - **THEN** it receives only a closed staged artifact whose format, schema, and requested assertions all passed |
| writer-engine | requirement | Writer Engine operation catalog | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:88 | The Writer Engine registry SHALL contain exactly these lifecycle operations. |
| writer-engine | scenario | Writer lifecycle operation in workflow | openspec/changes/implement-revised-data-toolkit-v1/specs/writer-engine/spec.md:99 | - **WHEN** a user `OperationCall` references a Writer Engine operation - **THEN** workflow validation returns `invalid_workflow` before source access |

+Exit code: 0
Wall time: 1.3 seconds
Output:
| v1-acceptance | requirement | CSV vertical-slice gate | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:3 | Before activating additional formats, the system SHALL pass an end-to-end CSV workflow containing a constant filter, at least one further logical operation, one reopened and schema-validated CSV output, immutable-source verification, deterministic-output verification, cancellation handling, and compact run-receipt verification. Inspection is verified inde... |
| v1-acceptance | scenario | Vertical slice success | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:11 | - **WHEN** the versioned vertical-slice fixture is executed twice with equivalent output destinations - **THEN** normalized outputs and explicit assertion results are deterministic, and both receipts identify the same source snapshot hash and workflow digest |
| v1-acceptance | requirement | Excel city-filter acceptance | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:16 | The system SHALL create a new Excel file from the approved `all_final.xlsx` source by filtering column `ישוב` to the constant `בת ים`, without changing the source, and SHALL validate the selected sheet, headers, row predicate, output style, and run receipt. |
| v1-acceptance | scenario | City-filter workflow | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:22 | - **WHEN** the approved local acceptance workflow runs - **THEN** every output data row has `ישוב = בת ים`, the output is a valid new workbook, and the source hash is unchanged |
| v1-acceptance | requirement | JSON field-extraction acceptance | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:26 | The system SHALL extract exactly `שם פרטי`, `שם מלא`, `entityID`, `TZ`, `ישוב`, and `מייל` from the approved JSON source into a new validated CSV output while preserving order, Unicode, null distinctions, and exact scalar values. |
| v1-acceptance | scenario | JSON-to-CSV workflow | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:31 | - **WHEN** the approved local extraction workflow runs - **THEN** the output header and data contain exactly the six fields in declared order and the source hash is unchanged |
| v1-acceptance | requirement | Cleanup-script scoped coverage acceptance | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:35 | V1 SHALL remain narrow and SHALL NOT claim full replacement or equivalence with `Invoke-RawMemberCleanup.Streaming.py`. The sanitized cleanup manifest SHALL preserve exactly four behaviors expressible through the fixed ten logical operations as V1 coverage evidence, and record the other fourteen as `outside_v1` for a future OpenSpec change that this change MUST NOT create. |
| v1-acceptance | scenario | Cleanup V1 scoped coverage | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:41 | - **WHEN** the four mapped cleanup fixtures are processed through registered V1 operations - **THEN** their recorded outputs and operation-defined failure behavior are met without importing or invoking the legacy script at runtime, while all fourteen `outside_v1` entries remain excluded from V1 acceptance |
| v1-acceptance | requirement | Codex reliability suite | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:45 | The project SHALL provide a versioned suite of at least 20 JSON, CSV, and Excel tasks covering Hebrew, null, exact numbers, dirty input, nested constant filters, formatting, clarification, rejection, output validation, and compact run receipts. |
| v1-acceptance | scenario | Luna 5.6 target | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:50 | - **WHEN** GPT-5.6 Luna at medium effort runs the suite - **THEN** all safety cases pass, at least 90 percent pass fully on the first attempt, and all pass within one structured-error correction |
| v1-acceptance | requirement | Baseline-derived performance thresholds | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:54 | The project SHALL record reproducible time and peak-memory baselines on an approximately 40 MB JSON fixture and a CSV fixture of up to 500,000 rows before setting numeric thresholds, then SHALL enforce the approved thresholds in repeatable performance checks. |
| v1-acceptance | scenario | Performance regression | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:60 | - **WHEN** a measured workflow exceeds an approved threshold under the recorded environment and method - **THEN** the performance gate fails with the measured value and threshold |
| v1-acceptance | requirement | Wide-fixture memory and spill gate | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:64 | The project SHALL include a non-sensitive wide fixture with 141 through 146 columns, 33 through 35 percent nulls, a text-heavy distribution, and hundreds of MB of data or an equivalent row count. A row-oriented baseline SHALL be recorded first. Columnar batches of 4K, 16K, or 64K rows are permitted only when measured allocations or garbage collection iden... |
| v1-acceptance | scenario | Bounded sort under pressure | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:86 | - **WHEN** stable `row.sort` or any mode of `row.deduplicate` exceeds its in-memory reservation - **THEN** it uses bounded spool or spill, honors cancellation and cleanup, or fails with `resource_limit_exceeded` without publication |
| v1-acceptance | scenario | Batch prototype fidelity | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:90 | - **WHEN** an internal 4K, 16K, or 64K batch prototype is benchmarked - **THEN** its semantic fidelity and first-error location match the row-oriented baseline before its performance result may be considered |
| v1-acceptance | requirement | Workflow template and CLI fixture gate | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:94 | Versioned fixtures SHALL cover contiguous variable IDs, multiple variables, defaults, invalid declared types, missing references, deterministic substitution and identical digest. They SHALL also cover an allowed config patch, a protected path, a denylist mutation attempt, precedence, and validation after every patch. |
| v1-acceptance | scenario | Protected configuration override | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:100 | - **WHEN** `--config-set` targets a protected path, its ancestor, or its descendant - **THEN** validation rejects it before planning and leaves the effective configuration unchanged |
| v1-acceptance | requirement | Reader, Writer, and logical lifecycle gate | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:104 | Lifecycle scenarios SHALL cover successful execution, Reader failure, Logical Engine failure, Writer failure, and cancellation. Each scenario after workspace creation SHALL observe `writer.finalize`, prescribed cleanup or retention, and exactly one receipt. Logical fixtures SHALL cover multi-key sort, null ordering, NFC Unicode ordering, stability, many u... |
| v1-acceptance | scenario | Writer failure finalization | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:112 | - **WHEN** staging, reopen validation, an assertion, or publication fails - **THEN** no partial output is reported and `writer.finalize` emits one terminal report and receipt after cleanup policy is applied |
| v1-acceptance | requirement | Google Sheets agent-bridge gate | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:116 | The Codex fixture suite SHALL cover unavailable connector, successful source export, successful native destination import, destination-folder move, remote collision, title and style policy, upload failure, local cleanup, and the prohibition on deleting a Drive file. Google Sheets is supported only through the Codex XLSX bridge and is not a runtime `Format... |
| v1-acceptance | scenario | Missing Google connector | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:123 | - **WHEN** a Google Sheets bridge run cannot discover the required Drive connector operation - **THEN** it stops before data processing and offers the documented manual XLSX fallback |
| v1-acceptance | requirement | Extension and architecture acceptance | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:127 | External tests SHALL add a data type, logical operation, and file format through descriptor, implementation, constructor, validation, and explicit registration without editing the orchestrator or another engine. The root module SHALL reject imports of `legacy/mvp` and forbidden outward dependencies. |
| v1-acceptance | scenario | Test-only extension | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:133 | - **WHEN** a valid test extension is registered - **THEN** discovery and dispatch expose it while duplicate or invalid registration is rejected |
| v1-acceptance | requirement | Repository quality gates | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:137 | Completion SHALL require formatting, focused contract tests, `go test ./...`, `go vet ./...`, `go build ./cmd/data-toolkit`, strict OpenSpec validation, stale scope/config searches, and documentation alignment. |
| v1-acceptance | scenario | Final implementation gate | openspec/changes/implement-revised-data-toolkit-v1/specs/v1-acceptance/spec.md:142 | - **WHEN** all implementation tasks are marked complete - **THEN** every required command passes and no active document advertises TXT, TUI, queries, Google Sheets as a native runtime format, or scaffold-only behavior as supported V1 functionality |

+Exit code: 0
Wall time: 1 seconds
Output:
| transform-workflow | requirement | Single-source single-output request | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:3 | `WorkflowRequest` SHALL contain one `SourceRequest`, an ordered list of registered `OperationCall` values, one `OutputRequest`, supported overrides, an exception policy, and explicit output assertions. V1 SHALL reject multiple sources, multiple outputs, queries, and unsupported fields. |
| transform-workflow | scenario | Second source rejected | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:9 | - **WHEN** a request contains more than one source - **THEN** schema validation fails before inspection or planning |
| transform-workflow | requirement | Registry-based workflow validation | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:13 | The orchestrator SHALL validate envelope version, identities, source and output formats, operation versions and parameters, capability requirements, schema references, configuration overrides, and V1 scope before execution. |
| transform-workflow | scenario | Invented capability | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:18 | - **WHEN** a request references a format or operation absent from runtime registries - **THEN** validation returns a structured capability error before source rows are read |
| transform-workflow | requirement | Minimal explicit output assertions | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:22 | V1 SHALL support exactly two user-declared assertion kinds: `exact_columns`, which declares the exact ordered stable column IDs expected in the output, and `all_rows_match`, which declares that every output row satisfies one valid constant-only filter expression. The Writer Engine SHALL always reopen the staged output and verify its format and declared sc... |
| transform-workflow | scenario | Row predicate assertion fails | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:31 | - **WHEN** a staged output contains a row that fails its declared `all_rows_match` expression - **THEN** output validation fails with an `OutputLocator` for the requested output path, zero-based row ordinal when row-specific, and optional column ID or physical position when available; no final output is published |
| transform-workflow | requirement | Ordered thin orchestration | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:35 | The orchestrator SHALL preserve declared logical-operation order, reject non-workflow operation exposure in user calls, and surround the declared calls with the fixed registered Reader and Writer Engine lifecycle. It SHALL NOT implement operation semantics, infer missing logical calls, search for plans, or solve constraints. |
| transform-workflow | scenario | Fixed engine ownership | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:41 | - **WHEN** a valid workflow is planned - **THEN** its logical calls retain request order and its fixed reader and writer lifecycle steps are selected from their registered descriptors without appearing as user calls |
| transform-workflow | requirement | Safe transform lifecycle | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:45 | Execution SHALL follow validated request and configuration resolution; shared read lock, immutable source snapshot and source hash; canonical read with schema and dirty-input validation; ordered transformations; staged rendering; optional style; close and reopen; mandatory format/schema validation; explicit output assertions; atomic publication; and `writ... |
| transform-workflow | scenario | Successful run | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:56 | - **WHEN** every step and validation succeeds - **THEN** exactly one new output is published and the final report and receipt identify it |
| transform-workflow | scenario | Mid-run failure | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:60 | - **WHEN** any operation fails after staging begins - **THEN** no partial final output is reported and cleanup or retention follows effective policy |
| transform-workflow | requirement | Cancellation and transient progress | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:64 | The orchestrator SHALL propagate cancellation and MAY emit transient progress to an in-process observer. It SHALL NOT persist a lifecycle-event stream or return one as part of the terminal run result. |
| transform-workflow | scenario | Cancellation during streaming | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:69 | - **WHEN** the request context is cancelled while rows are being consumed - **THEN** stream processing stops, resources close, no output is published, and the terminal receipt records cancellation |
| transform-workflow | requirement | Complete final report | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:73 | Every run SHALL return output status, mandatory and explicit assertion results, bounded findings or an exception summary when the effective policy requests them, a first failure location when available, structured errors, effective-configuration digest, source hash, capability versions, and run receipt identity. Findings and exceptions SHALL NOT be persis... |
| transform-workflow | scenario | Report after blocked dirty input | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:80 | - **WHEN** stream-time input validation finds mixed types and effective policy is `block` - **THEN** the report identifies the blocking source location and contains no published output |
| transform-workflow | requirement | Exact Transform Workflow structures | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:84 | The transform contract SHALL use these exact strict structures. `operations` contains only descriptors with `owner=logical_engine` and `exposure=workflow`. `WorkflowOverrides` SHALL NOT contain source paths, output paths, operation registrations, style definitions, secrets, or report-retention policy. Every override field is optional; an empty override ob... |
| transform-workflow | scenario | Strict operation call | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:122 | - **WHEN** an operation call omits its version, contains an unknown parameter, or references a lifecycle operation - **THEN** workflow validation returns `invalid_workflow` before source access |
| transform-workflow | requirement | Exact validation request structures | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:126 | V1 user assertions SHALL use `ValidationRequest`, a tagged union with exactly these variants. Each assertion object SHALL reject fields belonging to the other variant. Mandatory staged-output `format` and `schema` validations are not request objects and always run before these assertions. |
| transform-workflow | scenario | Unknown assertion field | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:139 | - **WHEN** an assertion contains an unknown kind or a field from another variant - **THEN** workflow validation rejects it before source access |
| transform-workflow | requirement | Exact deterministic execution plan | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:143 | The normalized plan SHALL contain these ordered steps and no inferred logical operation: `reader.snapshot`, `reader.read`, every request operation in order, `writer.write`, optional `writer.style` for Excel style output, `writer.validate`, `writer.publish`, and `writer.finalize`. `writer.finalize` runs after every terminal path; `writer.publish` runs only... |
| transform-workflow | scenario | Plan reproducibility | openspec/changes/implement-revised-data-toolkit-v1/specs/transform-workflow/spec.md:150 | - **WHEN** the same normalized workflow, descriptors, and effective configuration are planned twice - **THEN** step IDs, operation references, parameter digests, and order are byte-identical |

+Exit code: 0
Wall time: 1 seconds
Output:
| reader-engine | requirement | Explicit Reader Engine registry | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:3 | The Reader Engine SHALL register versioned reader-format descriptors and concrete implementations for source inspection and canonical reading. It SHALL publish only reader capabilities that are actually implemented. |
| reader-engine | scenario | Descriptor and implementation mismatch | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:8 | - **WHEN** a reader descriptor declares inspection or reading without its required implementation - **THEN** registration fails before the capability becomes discoverable |
| reader-engine | requirement | Deterministic inspection | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:12 | `reader.inspect` SHALL inspect a source without changing it or creating a data output. It SHALL return a common `InspectionReport` plus one format-specific details object containing bounded samples, stable column IDs, types, counts, findings, and required user decisions. Inspection is outside Transform Workflow; a run SHALL NOT make an additional full-sou... |
| reader-engine | scenario | Ambiguous source structure | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:19 | - **WHEN** delimiter, header, sheet, root path, type, or cleanliness cannot be selected deterministically - **THEN** inspection returns a blocking decision instead of guessing |
| reader-engine | requirement | Immutable source snapshot and canonical read | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:23 | After the orchestrator validates and plans a run, `reader.snapshot` SHALL acquire shared read access under the effective lock policy, create one immutable workspace snapshot, and calculate its SHA-256 while copying. `reader.read` SHALL consume only that completed snapshot, decode directly to `Schema + RowStream + Value`, and validate schema and dirty valu... |
| reader-engine | scenario | Source remains unchanged | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:30 | - **WHEN** a run succeeds, fails, or is cancelled after snapshot creation - **THEN** the source has only been opened for shared read access and the report hash identifies the exact processed bytes |
| reader-engine | scenario | Blocking dirty value | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:34 | - **WHEN** a value cannot be decoded as the selected schema type and the effective policy is `block` - **THEN** reading stops at the first precise source locator and no Writer Engine publication occurs |
| reader-engine | requirement | Deterministic source-format contracts | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:38 | CSV reader behavior SHALL be UTF-8/NFC with closed-set delimiter detection and lossless null distinction. JSON reading SHALL require an explicit or unambiguous scalar root path and SHALL not flatten or explode. Excel reading SHALL default to the first sheet, honor an explicit sheet, and read cached/displayed formula values without launching Excel. These r... |
| reader-engine | scenario | Unsupported nested JSON source | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:45 | - **WHEN** a JSON source requires implicit flattening or exploding to become tabular - **THEN** Reader Engine returns a capability error before any Logical Engine row is emitted |
| reader-engine | requirement | Exact reader request and descriptor structures | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:49 | The Reader Engine SHALL use the following exact strict structures. Paths are local paths and SHALL be normalized to absolute paths before request hashing. `SourceRequest` SHALL reject every format-specific field that does not apply to its resolved format. Runtime discovery SHALL publish only a descriptor whose declared capabilities have concrete registere... |
| reader-engine | scenario | Non-local source path | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:79 | - **WHEN** a source request contains a remote URL or a mutable write path - **THEN** strict request validation rejects it before snapshot acquisition |
| reader-engine | requirement | Exact inspection result structures | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:83 | Inspection SHALL return the shared fields plus exactly one format-specific details object selected by `format_id`. |
| reader-engine | scenario | Format detail exclusivity | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:124 | - **WHEN** an inspection report is serialized - **THEN** exactly one of `csv`, `json`, or `excel` is present and matches `format_id` |
| reader-engine | requirement | Exact immutable snapshot structure | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:128 | The Reader lifecycle SHALL produce the following exact internal structure. |
| reader-engine | scenario | Snapshot before row access | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:138 | - **WHEN** `reader.read` starts - **THEN** it receives only a completed closed snapshot whose size and SHA-256 were verified |
| reader-engine | requirement | Reader Engine operation catalog | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:142 | The Reader Engine registry SHALL contain exactly these operations. Lifecycle operations are selected by the orchestrator and SHALL NOT appear in user calls. |
| reader-engine | scenario | Lifecycle operation in workflow | openspec/changes/implement-revised-data-toolkit-v1/specs/reader-engine/spec.md:152 | - **WHEN** a user `OperationCall` references a Reader Engine operation - **THEN** workflow validation returns `invalid_workflow` before source access |

+Exit code: 0
Wall time: 0.8 seconds
Output:
| logical-operations | requirement | Explicit logical operation registry | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:3 | Each logical operation SHALL provide a versioned descriptor, parameter schema, constructor, validation, deterministic executor, input/output shape, streaming behavior, and explicit instance-owned registration. |
| logical-operations | scenario | Duplicate logical operation | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:8 | - **WHEN** two constructors register the same normalized operation identity and version - **THEN** the registry rejects the duplicate deterministically |
| logical-operations | requirement | Validate before consuming rows | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:12 | Every logical operation SHALL validate its complete parameter object and input schema before reading the first row from its input stream. |
| logical-operations | scenario | Unknown parameter | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:16 | - **WHEN** an operation call contains a parameter outside its strict schema - **THEN** validation fails and the input stream remains unconsumed |
| logical-operations | requirement | Projection and rename operations | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:20 | The catalog SHALL provide projection and rename operations that use stable column IDs, preserve requested column order, produce a new schema, and reject missing or duplicate output identities. |
| logical-operations | scenario | Project Hebrew columns | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:25 | - **WHEN** projection selects stable IDs for Hebrew-named source columns in a declared order - **THEN** the output schema and every row contain exactly those columns in that order |
| logical-operations | requirement | Constant-only Boolean filter | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:29 | The catalog SHALL provide a filter whose parameters are an explicit expression tree of `and`, `or`, `not`, and typed comparison leaves between one column and a constant. It SHALL reject column-to-column and cross-source comparisons. |
| logical-operations | scenario | Nested filter expression | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:34 | - **WHEN** a valid nested expression combines constant comparisons - **THEN** the operation evaluates the explicit tree without inferring precedence and preserves relative order of matching rows |
| logical-operations | scenario | Column comparison rejected | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:38 | - **WHEN** a comparison leaf identifies another column as its right operand - **THEN** validation fails before row consumption |
| logical-operations | requirement | Cleaning and normalization catalog | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:42 | The catalog SHALL provide leading/trailing trim, registered type or format normalization, scalar or regex replacement, delete-empty-row, keyed deduplication, and stable sorting with explicit parameters and deterministic findings. |
| logical-operations | scenario | Empty-field safety | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:47 | - **WHEN** a selected field is empty but another field in the row contains data - **THEN** delete-empty-row does not delete the row unless the operation's explicit whole-row condition matches |
| logical-operations | scenario | Stable deduplication | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:51 | - **WHEN** duplicate rows are found and `keep=first` is selected - **THEN** the first row in source order is retained and later duplicates are reported |
| logical-operations | requirement | Immutable streaming execution | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:55 | Logical operations SHALL never mutate input values, rows, schemas, or source files and SHALL wrap streams or use bounded deterministic spooling only when the declared operation requires it. |
| logical-operations | scenario | Changed value | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:60 | - **WHEN** normalization changes a value - **THEN** the output contains a newly constructed valid `Value` and the input value remains unchanged |
| logical-operations | scenario | Operation chain | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:64 | - **WHEN** several streamable operations are chained - **THEN** data is not round-tripped through an intermediate CSV file |
| logical-operations | requirement | Exact operation descriptor | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:68 | Every logical descriptor SHALL expose the following complete structure through `capabilities` and `describe`. |
| logical-operations | scenario | Incomplete descriptor | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:87 | - **WHEN** an operation omits a descriptor field or exposes a parameter schema with open additional properties - **THEN** registration fails before capability discovery |
| logical-operations | requirement | Exact typed literal and expression structures | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:91 | Filter and replacement contracts SHALL use a typed literal and a strict tagged expression union. Each expression node SHALL contain only the fields of its selected `op` family. Ordering comparisons require matching comparable data types. Text operators require `string`; `matches` uses Go RE2 syntax and a validated bounded pattern. |
| logical-operations | scenario | Invalid tagged expression | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:112 | - **WHEN** an expression mixes group, not, or comparison fields or uses a text operator on a non-string column - **THEN** operation validation fails before reading the first row |
| logical-operations | requirement | Complete V1 logical operation catalog | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:116 | The registry SHALL contain exactly these logical operations and parameter contracts. Every array preserves request order and rejects duplicate column IDs. It is not a replacement or equivalence contract for `Invoke-RawMemberCleanup.Streaming.py`: four mapped behaviors are V1 coverage evidence, and fourteen are `outside_v1` for a future OpenSpec change that this change MUST NOT create. |
| logical-operations | scenario | Catalog completeness | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:133 | - **WHEN** runtime capability discovery runs for the V1 composition root - **THEN** all ten logical IDs above are present once at `v1` and no excluded operation is registered |
| logical-operations | requirement | Exact logical parameter structures | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:137 | The ten operations SHALL use these exact parameter objects. `row.delete_empty` SHALL delete a row only when every cell in the row is either canonical null or an empty string. A literal hyphen, zero, or `false` is not empty. `row.deduplicate` SHALL use exact state and MAY remain entirely in memory only while the run budget permits. Every keep mode is `boun... |
| logical-operations | scenario | Delete-empty whole-row safety | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:170 | - **WHEN** one cell is null or empty but any other cell contains a non-empty canonical value - **THEN** `row.delete_empty` preserves the row |
| logical-operations | requirement | Operation failure detail catalog | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:174 | Non-resource logical operation failures return `operation_failed` with `operation_reason` limited to `invalid_parameters`, `column_not_found`, `duplicate_output_header`, `type_mismatch`, `invalid_regex`, or `dirty_value`. Resource exhaustion returns top-level `resource_limit_exceeded`, never `operation_failed` with `operation_reason=resource_limit_exceeded`; the error identifies the operation call ID and row-time failures include the first source locator. |
| logical-operations | scenario | Stable operation failure | openspec/changes/implement-revised-data-toolkit-v1/specs/logical-operations/spec.md:184 | - **WHEN** normalization encounters the same invalid value under `invalid_action=error` - **THEN** it returns `operation_failed`, reason `dirty_value`, and the same first source locator |

+Exit code: 0
Wall time: 0.8 seconds
Output:
| infrastructure-contracts | requirement | Canonical streaming table model | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:3 | The system SHALL represent transform data only as a validated `Schema`, an ordered `RowStream`, and typed immutable `Value` instances. A cell SHALL contain its column identity and value without per-cell provenance, lineage, or reverse mutation state. |
| infrastructure-contracts | scenario | Distinct null and text values | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:9 | - **WHEN** a source contains null, an empty string, and the literal string `-` - **THEN** the canonical stream preserves them as three distinct values |
| infrastructure-contracts | scenario | Exact numeric value | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:13 | - **WHEN** an adapter reads an integer or decimal that cannot round-trip through `float64` - **THEN** the canonical value preserves the exact numeric representation |
| infrastructure-contracts | scenario | Diagnostic location without cell provenance | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:17 | - **WHEN** a value produces an inspection finding or execution error - **THEN** the finding or error identifies the source location without adding provenance to every cell |
| infrastructure-contracts | requirement | Complete schema metadata | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:21 | The system SHALL model stable column identity, physical position, source header, optional subheader, alias, semantic type, data type, and type authority as schema metadata, and SHALL keep row values outside column descriptors. |
| infrastructure-contracts | scenario | Duplicate source headers | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:26 | - **WHEN** inspection finds duplicate source headers - **THEN** each column receives a distinct stable column ID and remains addressable without guessing by header text |
| infrastructure-contracts | requirement | Strict V1 configuration | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:30 | The system SHALL accept only the revised V1 configuration groups and exact fields defined by the product brief, SHALL reject unknown fields, and SHALL enforce UTF-8, NFC, source-hash reporting, and an effective-configuration digest in every run receipt. |
| infrastructure-contracts | scenario | Removed foundation field | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:36 | - **WHEN** configuration contains `output.txt_delimiter`, `boolean.not_scope`, or `interfaces.recent_workflows` - **THEN** validation fails before workflow planning with a field-specific error |
| infrastructure-contracts | scenario | Registered semantic type reference | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:40 | - **WHEN** a semantic type refers to an unregistered data type - **THEN** semantic validation rejects the configuration |
| infrastructure-contracts | requirement | Immutable configuration precedence | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:44 | The system SHALL resolve `embedded defaults -> user config -> CLI config patches -> workflow overrides -> output overrides` into a new typed `EffectiveConfig` without mutating any input layer. The normalized resolved configuration SHALL produce the effective-configuration digest used by validation, execution, reports, and receipts. |
| infrastructure-contracts | scenario | Output override wins | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:51 | - **WHEN** all five layers provide a supported output setting - **THEN** the effective configuration uses the output override and records its effective-configuration digest in the run receipt |
| infrastructure-contracts | requirement | Protected CLI configuration patches | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:55 | Each CLI patch SHALL be one strict `ConfigOverride{path,value}` applied in supplied order to a new configuration value. Duplicate paths, invalid JSON Pointers, nonexistent paths, type-changing or otherwise invalid structural changes, and any target equal to, above, or below a protected path SHALL fail before planning. Full schema and semantic validation S... |
| infrastructure-contracts | scenario | Protected descendant patch | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:68 | - **WHEN** a CLI patch targets a protected path, its ancestor, or its descendant - **THEN** configuration validation returns `invalid_config`, leaves every input layer unchanged, and does not plan the run |
| infrastructure-contracts | requirement | Versioned application envelopes | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:72 | The system SHALL expose typed versioned request and result envelopes for capability discovery, description, configuration validation, inspection, workflow validation, and execution, with a request ID and structured status. |
| infrastructure-contracts | scenario | Request correlation | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:77 | - **WHEN** an API operation returns success or failure - **THEN** its result contains the originating request ID and supported schema version |
| infrastructure-contracts | requirement | Structured deterministic errors | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:81 | The system SHALL return stable error codes, component, field or location when available, and human-readable detail, without embedding secrets or input row values unnecessarily. |
| infrastructure-contracts | scenario | Unknown operation | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:86 | - **WHEN** a workflow references an unregistered operation ID or version - **THEN** validation returns a structured capability error before source rows are consumed |
| infrastructure-contracts | requirement | Compact append-only run receipt | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:90 | The system SHALL append one terminal receipt per attempted run containing request ID, normalized-workflow digest, capability and binary versions, effective-configuration digest, source snapshot hash, published-output reference, terminal status, one structured terminal error when present, and timestamps. A receipt SHALL NOT contain per-cell lineage, a reve... |
| infrastructure-contracts | scenario | Failed run receipt | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:99 | - **WHEN** execution fails after a snapshot is created - **THEN** the receipt records the snapshot hash, failure, terminal error, and absence of a successfully published output |
| infrastructure-contracts | scenario | Secret redaction | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:103 | - **WHEN** a request or environment contains credential material - **THEN** no credential value is written to the receipt or ordinary report |
| infrastructure-contracts | requirement | Primitive identities and enums | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:107 | All contract objects SHALL use snake_case JSON names, `additionalProperties: false`, UTF-8 strings normalized to NFC, and the following fixed aliases and enums. An omitted optional field differs from an empty required field. |
| infrastructure-contracts | scenario | Unknown enum value | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:137 | - **WHEN** a request, descriptor, or configuration contains an enum value outside its fixed catalog - **THEN** strict validation rejects it before planning |
| infrastructure-contracts | requirement | Exact canonical data structures | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:141 | The root module SHALL implement the following exact semantic structures. Fields marked internal are not serialized on the JSON API.  Canonical null SHALL be a registered `DataTypeID` of `null`; empty string SHALL be `string` with zero encoded length in its type-owned representation, and the literal hyphen SHALL be an ordinary non-empty string. Decimal val... |
| infrastructure-contracts | scenario | Row/schema mismatch | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:183 | - **WHEN** a row has a different source, sheet, cell count, column order, or unregistered value type from its schema - **THEN** validation fails before that row reaches a logical operation |
| infrastructure-contracts | requirement | Exact descriptors and reporting structures | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:187 | Infrastructure SHALL define the following JSON structures for discovery, findings, terminal reports, and compact receipts. `RunReceipt` SHALL contain exactly: `receipt_id`, `request_id`, `run_id`, `normalized_workflow_digest`, `binary_version`, `capabilities_digest`, `effective_config_digest`, optional `source_snapshot_hash`, optional `published_output`, ... |
| infrastructure-contracts | scenario | Receipt compactness | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:265 | - **WHEN** a run terminates with findings and exceptions - **THEN** the `RunReport` may contain them but the `RunReceipt` contains only its exact compact field set `ConfigOverride` contains exactly `path:string` and `value:JSON value`. `path` is an existing JSON Pointer to a config value; duplicate paths are invalid. `WorkflowTemplateDocument` contains ex... |
| infrastructure-contracts | requirement | Exact configuration structures | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:284 | The strict YAML/JSON configuration SHALL use the following field catalog. Every object rejects additional properties. `ConfigDocument` is the root object in the table. `EffectiveConfig` is an immutable resolved `ConfigDocument` with the same fields and types and no extra JSON fields; its digest is carried separately in API and report structures. |
| infrastructure-contracts | scenario | Complete default configuration | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:353 | - **WHEN** embedded defaults are decoded - **THEN** every required field above is present, passes the same schema and semantic validation as user configuration, and resolves without mutation |
| infrastructure-contracts | requirement | Memory-budget enforcement | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:357 | Every run SHALL use a run-scoped resource budget covering reader buffers, logical-operation state, writer buffers, and bounded batches. Implementations must reserve material state before using it, release reservations promptly, and spill deterministically when an operation declares `bounded_spool`. A fixed batch size is forbidden. If the remaining budget ... |
| infrastructure-contracts | scenario | Unique keyed deduplication exceeds memory | openspec/changes/implement-revised-data-toolkit-v1/specs/infrastructure-contracts/spec.md:366 | - **WHEN** exact deduplication state exceeds the available budget and workspace spill cannot proceed - **THEN** the run terminates with `resource_limit_exceeded`, finalizes once, and publishes no output |

+Exit code: 0
Wall time: 0.6 seconds
Output:
| codex-adapter | requirement | Runtime-first Codex sequence | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:3 | The Codex Adapter SHALL instruct Codex to call capability discovery, describe candidate operations, inspect an unfamiliar or ambiguous source when needed, resolve required decisions, construct and validate a request, run it, and interpret the final report. It SHALL not require inspection before every otherwise complete repeat run. |
| codex-adapter | scenario | Supported natural-language task | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:10 | - **WHEN** a user asks for a task expressible by registered capabilities - **THEN** Codex uses the Data Toolkit binary and does not create a one-off data-processing script |
| codex-adapter | requirement | No duplicate capability catalog | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:14 | The Codex Skills SHALL describe procedure and decision rules but SHALL NOT embed an authoritative list of operation IDs, format IDs, or parameter schemas. |
| codex-adapter | scenario | Runtime catalog changes | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:18 | - **WHEN** a registered capability is added or removed - **THEN** Codex follows the new runtime discovery result without a skill catalog update |
| codex-adapter | requirement | Clarification instead of guessing | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:22 | The Codex Adapter SHALL require clarification when inspection or request validation identifies ambiguous sheet, delimiter, header, root path, duplicate column name, output target, or user intent that changes the result. |
| codex-adapter | scenario | Duplicate headers | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:27 | - **WHEN** inspection reports duplicate headers and the requested column cannot be identified uniquely - **THEN** Codex asks the user to select a stable column ID before execution |
| codex-adapter | requirement | Explicit unsupported-capability behavior | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:31 | Codex SHALL report a capability gap when the runtime cannot express the task and SHALL NOT invent descriptors, parameters, or successful results. It SHALL not silently bypass the toolkit with new processing code. |
| codex-adapter | scenario | Cross-source join request | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:36 | - **WHEN** the user requests a V1-excluded join - **THEN** Codex reports that the capability is unavailable and does not submit an invalid workflow |
| codex-adapter | requirement | Machine-error self-correction | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:40 | Codex SHALL use structured validation errors for at most one corrected request attempt when the user's intent is already clear; it SHALL ask for clarification when correction would require a user decision. |
| codex-adapter | scenario | Unknown request field | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:45 | - **WHEN** validation rejects a mechanically removable unknown field - **THEN** Codex corrects the request once and revalidates without changing user intent |
| codex-adapter | requirement | Versioned adapter fixtures | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:49 | The adapter SHALL include golden request and response fixtures for discovery, clarification, validation, execution, invented capability, unknown field, override precedence, and report interpretation. |
| codex-adapter | scenario | Skill regression | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:54 | - **WHEN** the skill or CLI contract changes - **THEN** its fixture suite detects any divergence from the supported sequence and safety rules |
| codex-adapter | requirement | Exact Codex Skill inventory | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:58 | The Codex Adapter SHALL contain exactly the following six project-local Skills. These Skills define procedure only; operation IDs, format IDs, and parameter schemas SHALL be read from `capabilities` and `describe` at runtime. |
| codex-adapter | scenario | Skill family discovery | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:72 | - **WHEN** project-local Skills are enumerated - **THEN** all six names and locations above exist and no additional Data Toolkit execution Skill competes with their responsibilities |
| codex-adapter | requirement | Root Skill contract | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:76 | `data-toolkit` SHALL accept the user's natural-language intent, source path, desired output, and any explicit preferences already supplied. It SHALL run `capabilities --json`, reject V1-excluded tasks, decide whether inspection is needed, detect a Google Spreadsheet source or target before local processing, and hand off without constructing operation para... |
| codex-adapter | scenario | Unsupported join | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:88 | - **WHEN** runtime discovery and V1 scope show that the requested task requires a join - **THEN** the root Skill returns `unsupported` without inventing a workflow or invoking a data-processing script |
| codex-adapter | requirement | Inspection Skill contract | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:92 | `data-toolkit-inspect` SHALL accept a `SourceRequest`, capabilities digest, and optional already-resolved decisions. It SHALL describe the selected format when needed, call `inspect`, and return the report unchanged plus a separate ordered list of decisions supplied by the user. It SHALL not edit the source or build a transform request. |
| codex-adapter | scenario | Ambiguous worksheet | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:99 | - **WHEN** inspection requires `select_sheet` - **THEN** the Skill asks one focused question using the reported options and does not hand off until the answer is resolved |
| codex-adapter | requirement | Workflow Builder Skill contract | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:103 | `data-toolkit-build-workflow` SHALL accept user intent, runtime descriptors, source inspection or known source contract, output target, and resolved preferences. For each candidate operation it SHALL call `describe`, construct only fields allowed by the returned schemas, call `workflow validate`, and return the validated request plus both digests. It SHAL... |
| codex-adapter | scenario | Unknown operation parameter | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:121 | - **WHEN** a desired parameter is absent from the runtime descriptor schema - **THEN** the Skill reports a capability gap instead of adding the field |
| codex-adapter | requirement | Run Skill contract | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:125 | `data-toolkit-run` SHALL accept the exact validated workflow-template file path, optional user-config path, ordered `--var` bindings, ordered `--config-set` patches, expected workflow digest, effective-configuration digest, capabilities digest, and user authorization implicit in the active request. It SHALL call `run --file` with the same template and bin... |
| codex-adapter | scenario | Successful run handoff | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:135 | - **WHEN** `run` returns a succeeded `RunReport` - **THEN** the Skill reports the exact published path and validation summary and does not invoke recovery |
| codex-adapter | requirement | Google Sheets bridge Skill contract | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:139 | `data-toolkit-google-sheets` SHALL be the only V1 Codex procedure that handles a Google Spreadsheet. Google Sheets SHALL NOT be a Go `FormatID`, runtime format provider, or toolkit network integration. Before processing it SHALL check that the callable Google Drive connector operations needed for the chosen direction are available: metadata lookup, export... |
| codex-adapter | scenario | Missing Drive connector | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:177 | - **WHEN** a source or destination is a Google Spreadsheet and a required Drive operation is not callable - **THEN** the Skill stops before any transformation, reports the missing capability, and offers manual XLSX export or verified XLSX upload as applicable |
| codex-adapter | scenario | Google destination succeeds | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:181 | - **WHEN** the local XLSX run, native import, parent move, and readbacks succeed - **THEN** the Skill reports the verified Spreadsheet link and receipt, deletes only its local XLSX artifact, and leaves the Drive Spreadsheet intact |
| codex-adapter | scenario | Remote title collision | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:185 | - **WHEN** an exact native Google Sheet with the resolved title already exists in the destination folder - **THEN** `block` stops before import, while `alternate_name` searches deterministically for the first available suffixed title before importing |
| codex-adapter | requirement | Recovery Skill contract | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:189 | `data-toolkit-recover` SHALL accept the original request, one `APIError`, and an attempt count. It SHALL run only when `attempt_count=0`, `retryable=true`, and the correction is mechanical: remove an unknown optional field, add an explicit registered version, normalize a syntactic request representation, or select the single unambiguous option already pre... |
| codex-adapter | scenario | One correction limit | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:200 | - **WHEN** the corrected request fails - **THEN** recovery stops and reports the second structured error without another mutation or run |
| codex-adapter | requirement | Skill input and output fixture schemas | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:204 | Every golden Skill fixture SHALL contain `skill`, `input`, `runtime_responses`, `expected_commands`, `expected_output_kind`, and `expected_output`. Fixtures SHALL cover every handoff, stop condition, and forbidden side effect. Runtime responses are recorded fixtures, not a static capability catalog in Skill text. Google bridge fixtures SHALL additionally ... |
| codex-adapter | scenario | Handoff regression | openspec/changes/implement-revised-data-toolkit-v1/specs/codex-adapter/spec.md:213 | - **WHEN** a Skill change alters a command, handoff payload, output kind, or stop condition - **THEN** the golden fixture fails before the Codex evaluation suite is run |

+Exit code: 0
Wall time: 1.3 seconds
Output:
| cli-json-surface | requirement | Required local commands | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:3 | The English CLI SHALL expose `capabilities --json`, `describe --kind operation --id <id> --json`, `config validate --request <file> --json`, `inspect --request <file> --json`, `workflow validate --file <workflow.json> [--config <config>] [--var <id>=<value>] [--config-set <json-pointer>=<json-value>] --json`, and `run --file <workflow.json> [--config <con... |
| cli-json-surface | scenario | Command inventory | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:12 | - **WHEN** CLI help is requested - **THEN** all required command families are present and no TUI, server, MCP, or remote-execution command is advertised as V1 |
| cli-json-surface | requirement | Runtime-derived discovery | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:16 | The `capabilities` and `describe` commands SHALL serialize sorted descriptors from the compiled runtime registries and SHALL NOT read a duplicated capability catalog from documentation or configuration. |
| cli-json-surface | scenario | Newly registered operation | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:21 | - **WHEN** the composition root registers a valid operation - **THEN** `capabilities` and `describe` expose it without changes to CLI command logic |
| cli-json-surface | requirement | JSON stdout isolation | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:25 | With `--json`, each command SHALL write exactly one complete versioned JSON result to stdout. Human diagnostics, progress, and unexpected runtime detail SHALL go to stderr. |
| cli-json-surface | scenario | Validation error | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:30 | - **WHEN** `workflow validate --json` rejects a request - **THEN** stdout remains valid machine-readable JSON and stderr contains no duplicate JSON result |
| cli-json-surface | requirement | Stable exit status | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:34 | The CLI SHALL return exit code zero only for a successful requested operation and SHALL map validation, capability, lock, execution, cancellation, and publication failures to documented non-zero exit behavior. |
| cli-json-surface | scenario | Unsupported operation exit | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:39 | - **WHEN** workflow validation finds an unsupported operation - **THEN** the command returns the documented validation or capability exit class and a structured JSON error |
| cli-json-surface | requirement | Thin interface boundary | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:43 | CLI code SHALL parse flags and files, call the application API, and render results without owning product rules, registries, workflow planning, or file adapter behavior. |
| cli-json-surface | scenario | API substitution test | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:48 | - **WHEN** a fake application API is provided to a CLI command test - **THEN** the command behavior is testable without real adapters or duplicated business logic |
| cli-json-surface | requirement | Workflow-template loading and CLI patches | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:52 | The CLI SHALL load only a strict `WorkflowTemplateDocument` for workflow validation and run; it SHALL NOT accept a long command that describes logical operations. It SHALL accept repeatable `--var <id>=<value>` and `--config-set <json-pointer>=<json-value>` flags, plus optional `--config <path>`. Variable bindings are parsed according to declared type; ob... |
| cli-json-surface | scenario | Protected configuration patch | openspec/changes/implement-revised-data-toolkit-v1/specs/cli-json-surface/spec.md:61 | - **WHEN** `--config-set` targets a protected pointer, its parent, or its child - **THEN** validation returns a structured configuration error before source access |

+Exit code: 0
Wall time: 1.1 seconds
Output:
| application-api | requirement | Common JSON envelope | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:3 | Every machine-facing command SHALL use the following exact JSON envelopes with `additionalProperties: false`. JSON names are normative; timestamps use UTC RFC3339Nano. |
| application-api | scenario | Exclusive result branch | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:19 | - **WHEN** a command finishes - **THEN** its envelope contains exactly one of `result` or `error` and preserves the request ID |
| application-api | requirement | Exact application API | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:23 | The in-process API SHALL expose exactly these V1 methods and SHALL use the same payload types as the JSON surface. An `APIError` is a product result; an ordinary Go `error` is reserved for a process-level failure that prevented construction of a valid result envelope.  |
| application-api | scenario | Thin interface client | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:40 | - **WHEN** the CLI invokes a product operation - **THEN** it calls one API method and does not reproduce validation, registry, planning, or adapter logic |
| application-api | requirement | Capability discovery contract | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:44 | `CapabilitiesRequest` SHALL be an empty strict object. `CapabilitiesResult` SHALL contain the exact fields below, sorted by normalized ID and version. |
| application-api | scenario | Runtime-only discovery | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:59 | - **WHEN** registration changes at composition time - **THEN** the discovery result and digest change without editing CLI or Codex Skill catalogs |
| application-api | requirement | Operation description contract | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:63 | `DescribeOperationRequest` SHALL contain `operation_id` and optional `version`. Omitted `version` selects the only registered version or fails if more than one is registered. The successful result SHALL be one complete `OperationDescriptor`, including its parameter JSON Schema. |
| application-api | scenario | Ambiguous operation version | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:74 | - **WHEN** the requested operation has multiple registered versions and no version is supplied - **THEN** the API returns `ambiguous_capability_version` |
| application-api | requirement | Configuration validation contract | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:78 | `ConfigValidationRequest` SHALL contain `config_path`; the path identifies one local YAML or JSON configuration document. `ConfigValidationResult` SHALL not echo the full configuration. |
| application-api | scenario | Unknown configuration field | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:91 | - **WHEN** the configuration contains a field outside the strict V1 schema - **THEN** the API returns `invalid_config` with the exact JSON field path |
| application-api | requirement | Inspection API contract | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:95 | `InspectionRequest` SHALL contain one `SourceRequest`, optional `user_config_path`, and bounded inspection limits. Its successful result SHALL be the exact `InspectionReport` defined by the Infrastructure and Reader Engine specifications. |
| application-api | scenario | Inspection needs a decision | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:108 | - **WHEN** a sheet, delimiter, header, root path, or duplicate header remains ambiguous - **THEN** inspection succeeds with `requires_decision=true` and structured decision items rather than guessing |
| application-api | requirement | Workflow validation API contract | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:112 | `WorkflowValidationRequest` SHALL contain one exact resolved `WorkflowRequest`, an optional user configuration path, and optional ordered `ConfigOverride[]`. The CLI resolves a `WorkflowTemplateDocument` before constructing this request. The result SHALL contain the normalized workflow digest and complete referenced descriptors without consuming source rows. |
| application-api | scenario | Validation does not read data rows | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:131 | - **WHEN** a structurally valid workflow is validated - **THEN** schemas, registries, paths, and parameters are checked without opening a canonical row stream |
| application-api | requirement | Run API contract | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:135 | `RunRequest` SHALL contain the previously valid `WorkflowRequest`, optional user configuration path, optional ordered `ConfigOverride[]`, and optional `expected_workflow_digest`. When the digest is provided it SHALL match current normalization before the source is opened. After a run workspace exists, every terminal execution outcome SHALL return one `Run... |
| application-api | scenario | Validate-run drift | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:161 | - **WHEN** `expected_workflow_digest` differs from the normalized current request - **THEN** the API returns `workflow_digest_mismatch` before source access |
| application-api | scenario | Finalized failed run | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:165 | - **WHEN** a Reader, Logical Engine, Writer, or cancellation failure occurs after workspace creation - **THEN** the API returns the complete finalized `RunReport` with one receipt, and the CLI maps its `first_failure` to the documented non-zero exit class |
| application-api | requirement | Stable API error catalog | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:169 | `APIError` uses eight exact fields. Its optional locator is exactly one `SourceLocator` or `OutputLocator`. `RunFailureContext` requires `run_id` and `status`; `receipt_id` is optional and appears only after receipt append. Error codes remain the fixed eighteen-value catalog. |
| application-api | scenario | Stable programmatic failure | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:195 | - **WHEN** the same invalid request is evaluated against the same registry and configuration - **THEN** the error code, component, field path, retryability, and ordered details are identical |
| application-api | requirement | CLI command and exit mapping | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:199 | The CLI SHALL map commands to API methods and exit classes exactly as follows. |
| application-api | scenario | JSON stdout on non-zero exit | openspec/changes/implement-revised-data-toolkit-v1/specs/application-api/spec.md:225 | - **WHEN** a JSON command ends with any non-zero product exit class - **THEN** stdout contains exactly one valid `ResultEnvelope` and diagnostics, if any, are written only to stderr For a finalized `run`, the CLI SHALL select the exit class from `RunReport.first_failure.code` or cancellation status while preserving the successful result-envelope branch th... |
