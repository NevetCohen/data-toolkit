# Legacy MVP implementation

This nested Go module preserves the partial `build-data-toolkit-mvp`
implementation as read-only reference material.

It is not part of the Data Toolkit V1 runtime, root module, tests, build, or
dependency graph. Run legacy commands only from this directory when historical
comparison is explicitly required.
